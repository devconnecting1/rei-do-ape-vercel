RewriteEngine On
# Permite URLs sem extensao: /api/orulo -> /api/orulo.php
# (mantem os fetch('api/...') do frontend funcionando no Apache/HostGator)
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d
RewriteRule ^([a-z-]+)$ $1.php [L,QSA]
